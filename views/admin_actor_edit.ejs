<!DOCTYPE html>
<html>
  <head>
    <title>Edycja Aktora</title>
    <link rel="stylesheet" href="/css/style.css" />
  </head>
  <body>
    <div class="container" style="max-width: 600px">
      <h1>Edytuj Dane Aktora</h1>

      <div class="card">
        <form action="/admin/actors/edit/<%= actor.id %>" method="POST">
          <label>Imię:</label>
          <input
            type="text"
            name="first_name"
            value="<%= actor.first_name %>"
            required
          />

          <label>Nazwisko:</label>
          <input
            type="text"
            name="last_name"
            value="<%= actor.last_name %>"
            required
          />

          <label>Zarobki (PLN):</label>
          <input
            type="number"
            step="0.01"
            name="base_salary"
            value="<%= actor.base_salary %>"
            required
          />

          <label>Biografia:</label>
          <textarea name="bio" rows="5"><%= actor.bio %></textarea>

          <div
            style="
              margin-top: 20px;
              display: flex;
              justify-content: space-between;
            "
          >
            <a href="/admin/actors" class="btn" style="background: #555"
              >Anuluj</a
            >
            <button type="submit">Zapisz Zmiany</button>
          </div>
        </form>
      </div>
    </div>
  </body>
</html>
